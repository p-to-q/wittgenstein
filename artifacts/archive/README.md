# Artifact Archive

Historical generated outputs that are useful as receipts but should not sit at
the repository root.

- [`manual-output/`](manual-output/) preserves the old root `output/` directory:
  loose video/SVG/HTML examples from manual rendering experiments.

Active showcase artifacts remain in [`../showcase/`](../showcase/). New local
run output should go under `artifacts/runs/<run-id>/` or a caller-specified
ignored output path.
